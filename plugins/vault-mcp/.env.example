# Path to your Obsidian vault (required)
VAULT_ROOT=/path/to/your/vault

# Comma-separated directory names to exclude from task file scanning
# These are matched against directory names (not full paths)
EXCLUDE_DIRS=.git,.obsidian,node_modules,.trash,areas/__metadata,areas/personal/routines
